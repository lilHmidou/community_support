{% extends 'base.html.twig' %}

{% block body %}

{% endblock %}

{% block title %}
    <h1>Ajouter un Témoignage</h1>
    {{ form_start(testimonyForm) }}
    {{ form_widget(testimonyForm) }}
    {{ form_end(testimonyForm) }}
    <a href="{{ path('temoignage_index') }}">Retour à la liste</a>

{% endblock %}